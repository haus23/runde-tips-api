<div class="toasts d-none">
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div data-type="{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
</div>